@model IEnumerable<Tuan4_MaPhucHuy_1911067288.Models.Sach>
@{
    ViewBag.Title = "ListSach";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>THÔNG TIN SÁCH</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th> Mã loại </th>
        <th> Tên sách</th>
        <th> Hình </th>
        <th> Giá bán</th>
        <th> Ngày cập nhật </th>
        <th> Số lượng tồn</th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@Html.DisplayFor(modelItem => item.maloai)</td>
            <td width="20%">@Html.DisplayFor(modelItem => item.tensach)</td>
            <td width="20%"><img src="@item.hinh" alt="hinh thumbnail"  style="width:90%;height:50%" /></td>
            <td>@Html.DisplayFor(modelItem => item.giaban)</td>
            <td width="20%">@Html.DisplayFor(modelItem => item.ngaycapnhat)</td>
            <td width="10%">@Html.DisplayFor(modelItem => item.soluongton)</td>
            <td width="40%">
                @Html.ActionLink("Edit", "Edit", new { id = item.masach }) |
                @Html.ActionLink("Detail", "Detail", new { id = item.masach }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.masach })
            </td>
        </tr>
    }
</table>

